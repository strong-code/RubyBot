class Notify

	def self.has_notice?(user)
		#should check the database and see if they have any waiting notices
	end

	def self.create_notice(user, message, user_to_notify)
		#create and store a notice for a specified username
	end

	def self.display_notice(user, message)
		#display a waiting notice to a user
	end

end